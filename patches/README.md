# Patches

Small, reviewable modifications to pinned upstream projects live here, grouped by target project.

Each patch subdirectory must contain a README describing its upstream base revision, application order, and validation. Do not store patched third-party source trees or generated diffs from unrelated working changes.
